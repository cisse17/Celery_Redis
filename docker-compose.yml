version: "3.8"

services:

  django:
    container_name: django
    build: 
      context: ./
      # context: "./django_celery"
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./:/usr/src/app/
    ports:
      - 8001:8000
    environment:
      - DEBUG=1
      - SECRET_KEY=fgrhjbhetyh')çè-ç_
      - ALLOWED_HOSTS=localhost,127.0.0.1
  
  
    

    


 